apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: atlasdb
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  clusterIP: None
